# aryal.in

Personal site for Sujay Aryal — software engineer, full-stack & AI.

Single static `index.html`, no build step. Fonts from Google Fonts; everything else is inline.
Served by GitHub Pages from `main`, custom domain via `CNAME`.

Edit `index.html`, commit, push. That's the whole workflow.
